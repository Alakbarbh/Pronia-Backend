@model List<Social>
@{
    ViewData["Title"] = "Social";
}

<div class="container my-5">
    <a class="btn btn-success text-white" asp-action="Create" style="float:right">Create</a>
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        Image
                    </th>
                    <th>
                        Operations
                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var social in Model)
                {
                    <tr>
                        <td class="py-1">
                            @Html.Raw(@social.Image)
                        </td>


                        <td>
                            <a class="btn btn-outline-info" asp-action="Detail" asp-route-id="@social.Id">Info</a>
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@social.Id">Edit</a>
                            <form style="display:inline-block" asp-action="Delete" asp-route-id="@social.Id" method="post">
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>











